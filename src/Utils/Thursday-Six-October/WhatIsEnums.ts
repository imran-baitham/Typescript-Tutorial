export {};

/*
   ==================={   TypeScript   }======================
*/

// ====   1. What is Enums ? ====

// Ans : An enum is a special "class" that represents a group of constants (unchangeable variables).
// Ans : Enums come in two flavors string and numeric. Lets start with numeric.

//  ====   1. What is enum in TypeScript ? ====

/* In TypeScript, enums, or enumerated types, are data structures of constant length that hold a set
 of constant values. Each of these constant values is known as a member of the enum. Enums are useful
 when setting properties or values that can only be a certain number of possible values. */
